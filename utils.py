"""
Utility functions for the Gemini Assistant application.
"""

import subprocess
import sys
from config import USE_NATIVE_TTS

# Function to speak text using macOS text-to-speech
def speak_text_macos(text):
    """
    Diese Funktion ist deaktiviert, da die Sprachausgabe entfernt wurde.
    Die Funktion bleibt als Platzhalter erhalten, um die Kompatibilität mit dem restlichen Code zu gewährleisten.
    """
    # Sprachausgabe deaktiviert
    return False